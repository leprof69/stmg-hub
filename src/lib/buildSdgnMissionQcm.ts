/**
 * @deprecated Ne plus utiliser pour les jeux.
 * Les QCM jeux viennent uniquement de SDGN_MISSION_QCM_CURATED (banque par chapitre, validee).
 * Generation future : champs qcm[] sur les exercices Missions ou enrichissement depuis manuel PDF.
 */
export function buildSdgnMissionQcmFromContent(): never[] {
  return [];
}
